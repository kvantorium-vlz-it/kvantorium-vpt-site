{
  "$schema": "https://turborepo.org/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "env": [".env.production", ".env"]
    },
    "dev": {
      "env": [".env.development", ".env"],
      "cache": true
    }
  }
}
