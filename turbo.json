{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dotEnv": [".env.production", ".env"]
    },
    "dev": {
      "dotEnv": [".env.development", ".env"],
      "cache": true
    }
  }
}
