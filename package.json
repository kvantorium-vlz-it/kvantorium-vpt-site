{
  "name": "kvantorium-vpt-site",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=v16.18.1"
  },
  "packageManager": "pnpm@7.15.0"
}
